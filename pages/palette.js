import Layout from "../components/layout";
import Palette from "../components/palette";

const Schema = () => {
  return (
    <Layout>
      <Palette />
    </Layout>
  );
};

export default Schema;
